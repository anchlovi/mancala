package com.bol.mancalaapp

import java.util.*

/**
 * Type alias for representing the unique identifier of a game in the Mancala application.
 *
 * `GameId` is an alias for [UUID], providing a more descriptive name in the context of the application
 * and ensuring consistency in the type of identifiers used for games.
 */
typealias GameId = UUID